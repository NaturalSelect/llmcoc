server:
  host: "0.0.0.0"
  port: 8080

database:
  path: "data/llmcoc.db"

jwt:
  secret: "change-me-to-a-long-random-secret-in-production"
  expire_hours: 168   # 7 days

shop:
  initial_coins: 600
  initial_card_slots: 3
